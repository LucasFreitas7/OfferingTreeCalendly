/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['d3v0px0pttie1i.cloudfront.net', 'media.licdn.com', 's3.sa-east-1.amazonaws.com'],
  },
};

module.exports = nextConfig;
